config system admin
  edit "admin"
    unset password
end
config system admin
  edit "admin"
    set password "AppSec-Xp3rts2025!"
end
config system global
  set config-sync-enable enable
  set config-sync-port 995
end
config config sync-list
  edit "SyncList"
    set server-ip 10.1.2.51
    set port 995
    set password "AppSec-Xp3rts2025!"
    set type system log security share-resource user 
    unset comment
  next
end
config system interface
    edit port1
        set mode static
    next
    edit port1
        set ip 10.1.2.50/24
        set allowaccess http https ssh ping
        set secondary-ip enable
        config  secondary-ip-list
            edit 1
                set ip 0.0.0.0/0
                unset allowaccess
                set floating disable
                set traffic-group default
            next
            edit 2
                set ip 0.0.0.0/0
                unset allowaccess
                set floating disable
                set traffic-group default
            next
            edit 3
                set ip 0.0.0.0/0
                unset allowaccess
                set floating disable
                set traffic-group default
            next
        end
    next
    edit "port2"
        set ip 10.1.3.100/24
        set allowaccess https ping ssh http 
    next
end
config router static
    edit 1
        set destination 0.0.0.0/0
        set gateway 10.1.2.1
end
config system ha
  set mode active-active-vrrp
  set hbdev port1 
  set datadev port1 
  set group-id 1
  set local-node-id 0
  set group-name grp1
  set mgmt-status disable
  set priority 4
  set config-priority 50
  set override enable
  set hb-interval 2
  set arps 5
  set hb-lost-threshold 6
  set arps-interval 6
  set l7-persistence-pickup enable
  set l4-persistence-pickup enable
  set l4-session-pickup enable
  set auto-config-sync disable
  set remote-ip-monitor disable
  set boot-time 30
  set ha-eth-type 8890
  set hatrans-eth-type 8892
  set l2ep-eth-type 8893
  set hb-type unicast
  set local-address 10.1.2.50
  set peer-address 10.1.2.51 
end
